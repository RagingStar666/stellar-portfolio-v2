---
import "@styles/global.css"
import BaseHead from "@components/BaseHead.astro"
import Header from "@components/Header.astro"
import Footer from "@components/Footer.astro"
import Drawer from "@components/Drawer.astro"
const { title, description } = Astro.props
import { SITE } from "@consts"
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={`${title} | ${SITE.TITLE}`} description={description} />
    <script is:inline src="/js/particles.min.js"></script>
  </head>
  <body class="bg-midnight-black text-white">
    <!-- Particles.js Container -->
    <div id="particles-js" class="fixed inset-0 z-0 pointer-events-none"></div>
    
    <!-- Hide Light Mode Particles (keeping structure for potential reuse) -->
    <div class='absolute inset-0 hidden'>
      <div id='particles1' class='fixed inset-0'></div>
      <div id='particles2' class='fixed inset-0'></div>
      <div id='particles3' class='fixed inset-0'></div>
    </div>

    <!-- Dark Theme: Stars - Also Hidden -->
    <div class='absolute inset-0 hidden'>
      <div id='stars1' class='fixed inset-0'></div>
      <div id='stars2' class='fixed inset-0'></div>
      <div id='stars3' class='fixed inset-0'></div>
    </div>
    
    <Header />
    <Drawer />
    <main class="relative z-10">
      <slot />
    </main>
    <Footer />
    
    <script is:inline src="/js/bg.js"></script>
  </body>
</html>

<style>
  .bg-midnight-black {
    background-color: #000000;
  }
</style>
