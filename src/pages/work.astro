---
import { getCollection } from "astro:content"
import PageLayout from "@layouts/PageLayout.astro"
import { WORK } from "@consts"

// Get work experience entries
const workEntries = await getCollection("work");

// Organize entries by category
const professionalExperience = workEntries.filter(entry => 
  entry.id === "genesis-studio.md" || 
  entry.id === "genesis-team-leader.md" || 
  entry.id === "wuxiaworld.md"
).sort((a, b) => {
  // Parse dates for sorting (format: "Month Year")
  const getDate = (entry: typeof workEntries[0]): number => {
    const dateStartStr = typeof entry.data.dateStart === 'string' 
      ? entry.data.dateStart 
      : entry.data.dateStart.toString();
    const parts = dateStartStr.split(" ");
    const month = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"].indexOf(parts[0]);
    const year = parseInt(parts[1]);
    return new Date(year, month).getTime();
  };
  
  return getDate(b) - getDate(a); // Descending order
});

// Internship data
const internshipData = [
  {
    title: "Jus Scriptum",
    role: "Legal Research & Writing Intern",
    period: "December 2024 – January 2025",
    dateStart: "Dec 2024",
    description: "Conducted in-depth legal research and contributed to legal articles focusing on constitutional developments and intellectual property jurisprudence in India.",
    image: "/placeholder-image.svg",
    skills: ["Legal Research", "Constitutional Law", "IP Law", "Academic Writing"]
  },
  {
    title: "Jus Corpus",
    role: "Legal Writing Intern",
    period: "January – February 2025",
    dateStart: "Jan 2025",
    description: "Wrote analytical pieces on legal developments and case law summaries for publication on an open-access legal platform.",
    image: "/placeholder-image.svg",
    skills: ["Legal Writing", "Case Analysis", "Legal Research", "Content Creation"]
  }
].sort((a, b) => {
  // Sort by date (newest first)
  const getDate = (entry: {dateStart: string}): number => {
    const dateStart = entry.dateStart;
    const parts = dateStart.split(" ");
    const month = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"].indexOf(parts[0]);
    const year = parseInt(parts[1]);
    return new Date(year, month).getTime();
  };
  
  return getDate(b) - getDate(a); // Descending order
});

// Committee data
const committeeData = [
  {
    title: "Journal and Seminar Committee",
    role: "General Member",
    period: "2024 - Present",
    dateStart: "Jun 2024",
    description: "Active member of the Journal and Seminar Committee at the Department of Law, University of Calcutta, contributing to academic publications and event planning.",
    image: "/placeholder-image.svg",
    responsibilities: [
      "Assist in organizing academic seminars and conferences",
      "Review submitted articles for journal publication",
      "Coordinate with faculty and guest speakers",
      "Promote committee activities among student body"
    ]
  }
];

// Competition & extracurricular data
const extracurricularData = [
  {
    title: "3rd National Article Writing Competition",
    organizer: "Journal and Seminar Committee, Department of Law, University of Calcutta",
    role: "Participant",
    period: "December 2024",
    dateStart: "Dec 2024",
    description: "Participated in the national-level legal writing competition focused on contemporary legal issues.",
    image: "/placeholder-image.svg"
  },
  {
    title: "SKMMIMNC 6.0 (International Mediation and Negotiation Competition)",
    organizer: "Lloyd Law College, India",
    role: "Mediator",
    period: "March 2025",
    dateStart: "Mar 2025",
    description: "Served as a mediator in this prestigious international competition, applying ADR principles in simulated dispute scenarios.",
    image: "/placeholder-image.svg"
  },
  {
    title: "Article Writing Competition (2025 Edition)",
    organizer: "NUJS IPLTS in collaboration with The Dialogue",
    role: "Participant",
    period: "2025",
    dateStart: "Feb 2025",
    description: "Contributed a research article on intellectual property rights in the digital age.",
    image: "/placeholder-image.svg"
  }
].sort((a, b) => {
  // Sort by date (newest first)
  const getDate = (entry: {dateStart: string}): number => {
    const dateStart = entry.dateStart;
    const parts = dateStart.split(" ");
    const month = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"].indexOf(parts[0]);
    const year = parseInt(parts[1]);
    return new Date(year, month).getTime();
  };
  
  return getDate(b) - getDate(a); // Descending order
});

// Format professional entries
const professionalTimelineEntries = professionalExperience.map(entry => {
  const dateStartStr = typeof entry.data.dateStart === 'string' 
    ? entry.data.dateStart 
    : entry.data.dateStart.toString();
  
  const dateEndStr = entry.data.dateEnd === "Now" 
    ? "Present" 
    : (typeof entry.data.dateEnd === 'string' 
        ? entry.data.dateEnd 
        : entry.data.dateEnd.toString());
    
  return {
    type: "professional" as const,
    title: entry.data.company,
    role: entry.data.role,
    period: `${dateStartStr} - ${dateEndStr}`,
    dateStart: dateStartStr,
    dateEnd: dateEndStr,
    description: entry.body,
    image: "/placeholder-image.svg",
    slug: entry.slug
  };
});

// Format internship entries
const internshipTimelineEntries = internshipData.map(entry => ({
  type: "internship" as const,
  title: entry.title,
  role: entry.role,
  period: entry.period,
  dateStart: entry.dateStart,
  description: entry.description,
  image: entry.image,
  skills: entry.skills,
  slug: entry.title.toLowerCase().replace(/\s+/g, '-')
}));

// Format committee entries
const committeeTimelineEntries = committeeData.map(entry => ({
  type: "committee" as const,
  title: entry.title,
  role: entry.role,
  period: entry.period,
  dateStart: entry.dateStart,
  description: entry.description,
  image: entry.image,
  responsibilities: entry.responsibilities,
  slug: entry.title.toLowerCase().replace(/\s+/g, '-')
}));

// Format extracurricular entries
const extracurricularTimelineEntries = extracurricularData.map(entry => ({
  type: "extracurricular" as const,
  title: entry.title,
  organizer: entry.organizer,
  role: entry.role,
  period: entry.period,
  dateStart: entry.dateStart,
  description: entry.description,
  image: entry.image,
  slug: entry.title.toLowerCase().replace(/\s+/g, '-')
}));
---

<PageLayout title={WORK.TITLE} description={WORK.DESCRIPTION}>
  <div class="container mx-auto p-6">
    <h1 class="text-3xl font-bold mb-8 text-black dark:text-white border-b pb-4">
      {WORK.TITLE}
    </h1>
    
    <!-- Navigation Links -->
    <div class="flex flex-wrap gap-4 mb-8">
      <a href="#professional" class="px-4 py-2 bg-black/5 dark:bg-white/10 rounded-lg hover:bg-black/10 hover:dark:bg-white/20 transition-colors">
        Professional Experience
      </a>
      <a href="#internships" class="px-4 py-2 bg-black/5 dark:bg-white/10 rounded-lg hover:bg-black/10 hover:dark:bg-white/20 transition-colors">
        Internships
      </a>
      <a href="#committees" class="px-4 py-2 bg-black/5 dark:bg-white/10 rounded-lg hover:bg-black/10 hover:dark:bg-white/20 transition-colors">
        Committees
      </a>
      <a href="#extracurricular" class="px-4 py-2 bg-black/5 dark:bg-white/10 rounded-lg hover:bg-black/10 hover:dark:bg-white/20 transition-colors">
        Competitions & Activities
      </a>
    </div>
    
    <!-- Professional Experience Section -->
    <section id="professional" class="scroll-mt-24 mb-16">
      <h2 class="text-2xl font-semibold mb-6 text-black dark:text-white border-b border-black/10 dark:border-white/10 pb-2">
        Professional Experience
      </h2>
      
      <div class="relative">
        <!-- Timeline line on the left -->
        <div class="absolute left-16 top-0 bottom-0 w-0.5 bg-black/20 dark:bg-white/20"></div>
        
        <div class="space-y-8">
          {professionalTimelineEntries.map((entry) => (
            <div class="ml-24 relative">
              <!-- Timeline dot -->
              <div class="absolute left-[-8px] top-6 w-4 h-4 rounded-full bg-black dark:bg-white"></div>
              
              <!-- Timeline date -->
              <div class="absolute left-[-60px] top-5 text-sm font-medium text-black/70 dark:text-white/70">
                {entry.dateStart}
              </div>
              
              <!-- Content card -->
              <a href={`/work/${entry.slug}`} class="block bg-white dark:bg-black/20 rounded-lg shadow-sm border border-black/10 dark:border-white/10 hover:shadow-md transition-all duration-300 overflow-hidden">
                <div class="p-6">
                  <div class="flex flex-col md:flex-row gap-6">
                    <div class="md:w-1/4">
                      <img src={entry.image} alt={entry.title} class="w-full rounded-lg object-cover aspect-video" />
                    </div>
                    <div class="md:w-3/4">
                      <div class="mb-2">
                        <span class="inline-block px-2 py-1 bg-black/10 dark:bg-white/10 rounded-full text-xs text-black/70 dark:text-white/70">
                          Professional
                        </span>
                      </div>
                      <h3 class="text-xl font-semibold text-black dark:text-white">{entry.role}</h3>
                      <p class="text-black/70 dark:text-white/70 mb-1">{entry.title}</p>
                      <p class="text-black/60 dark:text-white/60 mb-3">{entry.period}</p>
                      <p class="text-black/80 dark:text-white/80 line-clamp-3">{entry.description}</p>
                      <div class="mt-3">
                        <span class="text-sm font-medium text-black dark:text-white hover:underline">Read more →</span>
                      </div>
                    </div>
                  </div>
                </div>
              </a>
            </div>
          ))}
        </div>
      </div>
    </section>
    
    <!-- Internships Section -->
    <section id="internships" class="scroll-mt-24 mb-16">
      <h2 class="text-2xl font-semibold mb-6 text-black dark:text-white border-b border-black/10 dark:border-white/10 pb-2">
        Internships
      </h2>
      
      <div class="relative">
        <!-- Timeline line on the left -->
        <div class="absolute left-16 top-0 bottom-0 w-0.5 bg-black/20 dark:bg-white/20"></div>
        
        <div class="space-y-8">
          {internshipTimelineEntries.map((entry) => (
            <div class="ml-24 relative">
              <!-- Timeline dot -->
              <div class="absolute left-[-8px] top-6 w-4 h-4 rounded-full bg-black dark:bg-white"></div>
              
              <!-- Timeline date -->
              <div class="absolute left-[-60px] top-5 text-sm font-medium text-black/70 dark:text-white/70">
                {entry.dateStart}
              </div>
              
              <!-- Content card -->
              <a href={`/work/internships/${entry.slug}`} class="block bg-white dark:bg-black/20 rounded-lg shadow-sm border border-black/10 dark:border-white/10 hover:shadow-md transition-all duration-300 overflow-hidden">
                <div class="p-6">
                  <div class="flex flex-col md:flex-row gap-6">
                    <div class="md:w-1/4">
                      <img src={entry.image} alt={entry.title} class="w-full rounded-lg object-cover aspect-video" />
                    </div>
                    <div class="md:w-3/4">
                      <div class="mb-2">
                        <span class="inline-block px-2 py-1 bg-black/10 dark:bg-white/10 rounded-full text-xs text-black/70 dark:text-white/70">
                          Internship
                        </span>
                      </div>
                      <h3 class="text-xl font-semibold text-black dark:text-white">{entry.role}</h3>
                      <p class="text-black/70 dark:text-white/70 mb-1">{entry.title}</p>
                      <p class="text-black/60 dark:text-white/60 mb-3">{entry.period}</p>
                      <p class="text-black/80 dark:text-white/80 line-clamp-3">{entry.description}</p>
                      <div class="mt-3">
                        <span class="text-sm font-medium text-black dark:text-white hover:underline">Read more →</span>
                      </div>
                    </div>
                  </div>
                </div>
              </a>
            </div>
          ))}
        </div>
      </div>
    </section>
    
    <!-- Committees Section -->
    <section id="committees" class="scroll-mt-24 mb-16">
      <h2 class="text-2xl font-semibold mb-6 text-black dark:text-white border-b border-black/10 dark:border-white/10 pb-2">
        Committees
      </h2>
      
      <div class="relative">
        <!-- Timeline line on the left -->
        <div class="absolute left-16 top-0 bottom-0 w-0.5 bg-black/20 dark:bg-white/20"></div>
        
        <div class="space-y-8">
          {committeeTimelineEntries.map((entry) => (
            <div class="ml-24 relative">
              <!-- Timeline dot -->
              <div class="absolute left-[-8px] top-6 w-4 h-4 rounded-full bg-black dark:bg-white"></div>
              
              <!-- Timeline date -->
              <div class="absolute left-[-60px] top-5 text-sm font-medium text-black/70 dark:text-white/70">
                {entry.dateStart}
              </div>
              
              <!-- Content card -->
              <a href={`/work/committees/${entry.slug}`} class="block bg-white dark:bg-black/20 rounded-lg shadow-sm border border-black/10 dark:border-white/10 hover:shadow-md transition-all duration-300 overflow-hidden">
                <div class="p-6">
                  <div class="flex flex-col md:flex-row gap-6">
                    <div class="md:w-1/4">
                      <img src={entry.image} alt={entry.title} class="w-full rounded-lg object-cover aspect-video" />
                    </div>
                    <div class="md:w-3/4">
                      <div class="mb-2">
                        <span class="inline-block px-2 py-1 bg-black/10 dark:bg-white/10 rounded-full text-xs text-black/70 dark:text-white/70">
                          Committee
                        </span>
                      </div>
                      <h3 class="text-xl font-semibold text-black dark:text-white">{entry.role}</h3>
                      <p class="text-black/70 dark:text-white/70 mb-1">{entry.title}</p>
                      <p class="text-black/60 dark:text-white/60 mb-3">{entry.period}</p>
                      <p class="text-black/80 dark:text-white/80 line-clamp-3">{entry.description}</p>
                      <div class="mt-3">
                        <span class="text-sm font-medium text-black dark:text-white hover:underline">Read more →</span>
                      </div>
                    </div>
                  </div>
                </div>
              </a>
            </div>
          ))}
        </div>
      </div>
    </section>
    
    <!-- Extracurricular Section -->
    <section id="extracurricular" class="scroll-mt-24">
      <h2 class="text-2xl font-semibold mb-6 text-black dark:text-white border-b border-black/10 dark:border-white/10 pb-2">
        Competitions & Activities
      </h2>
      
      <div class="relative">
        <!-- Timeline line on the left -->
        <div class="absolute left-16 top-0 bottom-0 w-0.5 bg-black/20 dark:bg-white/20"></div>
        
        <div class="space-y-8">
          {extracurricularTimelineEntries.map((entry) => (
            <div class="ml-24 relative">
              <!-- Timeline dot -->
              <div class="absolute left-[-8px] top-6 w-4 h-4 rounded-full bg-black dark:bg-white"></div>
              
              <!-- Timeline date -->
              <div class="absolute left-[-60px] top-5 text-sm font-medium text-black/70 dark:text-white/70">
                {entry.dateStart}
              </div>
              
              <!-- Content card -->
              <a href={`/work/activities/${entry.slug}`} class="block bg-white dark:bg-black/20 rounded-lg shadow-sm border border-black/10 dark:border-white/10 hover:shadow-md transition-all duration-300 overflow-hidden">
                <div class="p-6">
                  <div class="flex flex-col md:flex-row gap-6">
                    <div class="md:w-1/4">
                      <img src={entry.image} alt={entry.title} class="w-full rounded-lg object-cover aspect-video" />
                    </div>
                    <div class="md:w-3/4">
                      <div class="mb-2">
                        <span class="inline-block px-2 py-1 bg-black/10 dark:bg-white/10 rounded-full text-xs text-black/70 dark:text-white/70">
                          Competition
                        </span>
                      </div>
                      <h3 class="text-xl font-semibold text-black dark:text-white">{entry.role}</h3>
                      <p class="text-black/70 dark:text-white/70 mb-1">{entry.title}</p>
                      <p class="text-black/60 dark:text-white/60 mb-1">{entry.organizer}</p>
                      <p class="text-black/60 dark:text-white/60 mb-3">{entry.period}</p>
                      <p class="text-black/80 dark:text-white/80 line-clamp-3">{entry.description}</p>
                      <div class="mt-3">
                        <span class="text-sm font-medium text-black dark:text-white hover:underline">Read more →</span>
                      </div>
                    </div>
                  </div>
                </div>
              </a>
            </div>
          ))}
        </div>
      </div>
    </section>
  </div>
</PageLayout> 